trigger phrases: "design me a website", "write HTML"

## Writing HTML
Unless otherwise specified, include Tailwind CSS: `<script src="https://cdn.tailwindcss.com"></script>` then style the HTML with inline Tailwind CSS classes.
Running `html` will automatically open it in the user's browser, you don't need to tell them to do that.